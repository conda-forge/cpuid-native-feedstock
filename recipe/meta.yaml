{% set name = "cpuid-native" %}
{% set version = "0.0.9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/cpuid_native-{{ version }}.tar.gz
  sha256: 84e568520515a49bddc6a2afc495649bfbad017db52b895740d3f743bcb3a3b0

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
  host:
    - pip
    - python
  run:
    - python

test:
  imports:
    - cpuid_native
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/fpelliccioni/cpuid-py-native
  summary: x86 cpuid native API
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - thewchan
